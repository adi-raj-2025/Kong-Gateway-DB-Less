return {
    name = "sensitive-encrypt",
    fields = {
        {
            config = {
                type = "record",
                fields = {
                    -- Add any configuration fields here if needed in the future
                    -- For now, we'll leave it empty since you don't need configuration
                }
            }
        }
    }
}